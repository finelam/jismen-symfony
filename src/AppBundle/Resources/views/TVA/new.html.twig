{% extends '::adminbase.html.twig' %}

{% block body -%}
    <h1>Nouvelle TVA</h1>

    {{ form(form) }}

        <a href="{{ path('tva') }}" class="btn btn-jismen">
            Retour à la liste
        </a>
{% endblock %}
