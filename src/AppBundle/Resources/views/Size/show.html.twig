{% extends '::adminbase.html.twig' %}

{% block body -%}
    <h1>Taille</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>#</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ entity.name }}</td>
            </tr>
        </tbody>
    </table>
    <div class="btn-group">
        <a href="{{ path('size') }}" class="btn btn-primary">
            Retour à la liste
        </a>
        <a href="{{ path('size_edit', { 'id': entity.id }) }}" class="btn btn-warning">
            Modifier
        </a>
    </div>
{% endblock %}
