{% extends '::adminbase.html.twig' %}

{% block body -%}
    <h1>Nouvel utilisateur</h1>

    {{ form(form) }}

        <a href="{{ path('user') }}" class="btn btn-jismen">
            Retour à la liste
        </a>
{% endblock %}
