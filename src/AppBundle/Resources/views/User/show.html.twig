{% extends '::adminbase.html.twig' %}

{% block body -%}
    <h1>Utilisateur</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>#</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>
    <div class="btn-group">
        <a href="{{ path('user') }}" class="btn btn-primary">
            Retour à la liste
        </a>
        <a href="{{ path('user_edit', { 'id': entity.id }) }}" class="btn btn-warning">
            Edit
        </a>
    </div>
{% endblock %}
