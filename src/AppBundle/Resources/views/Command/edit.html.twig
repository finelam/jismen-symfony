{% extends '::adminbase.html.twig' %}

{% block body -%}
    <h1>Modifier</h1>

    {{ form(edit_form) }}

        <a href="{{ path('command') }}">
            Retour à la liste
        </a>
    {{ form(delete_form) }}
{% endblock %}
