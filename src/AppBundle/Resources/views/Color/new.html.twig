{% extends '::adminbase.html.twig' %}

{% block body -%}
    <h1>Nouvelle couleur</h1>

    {{ form(form) }}

    <a href="{{ path('color') }}" class="btn btn-jismen">
        Retour à la liste
    </a>
{% endblock %}
